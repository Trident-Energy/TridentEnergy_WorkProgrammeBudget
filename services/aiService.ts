// AI Service removed.
export const aiService = {
  // Placeholder to prevent import errors if still referenced
  generateDraft: async (): Promise<string> => "",
  validateBudget: async (): Promise<string> => ""
};